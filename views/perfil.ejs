<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <%- include('partials/head', {title: 'Perfil'}) %>
    <style>
        body {
            background-color: #0047CC;
            padding-bottom: 70px;
        }
        .page-title {
            color: white;
            font-size: 26px;
            font-weight: bold;
            margin: 20px 0 20px 20px;
        }
        .profile-card {
            background: white;
            border-radius: 15px;
            margin: 15px;
            padding: 20px;
        }
        .profile-header {
            display: flex;
            align-items: center;
            margin-bottom: 20px;
        }
        .profile-pic {
            width: 80px;
            height: 80px;
            border-radius: 10px;
            background-color: #0047CC;
            margin-right: 15px;
            overflow: hidden;
        }
        .profile-pic img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }
        .profile-info {
            flex-grow: 1;
        }
        .profile-name {
            font-size: 20px;
            font-weight: bold;
            margin-bottom: 5px;
            display: flex;
            align-items: center;
        }
        .profile-id {
            color: #ccc;
            font-size: 18px;
            font-weight: normal;
            margin-left: 10px;
        }
        .profile-details {
            color: #666;
            font-size: 14px;
            margin-top: 5px;
        }
        .user-groups {
            margin-top: 15px;
            font-weight: bold;
        }
        .empty-groups {
            color: #999;
            text-align: center;
            padding: 15px 0;
            font-size: 15px;
        }
        .history-card {
            background: white;
            border-radius: 15px;
            margin: 15px;
            padding: 20px;
        }
        .history-title {
            color: #0047CC;
            font-size: 22px;
            font-weight: bold;
            margin-bottom: 15px;
        }
        .empty-history {
            text-align: center;
            color: #999;
            padding: 25px 0;
            font-size: 16px;
        }
        .edit-profile-btn {
            background-color: #f5f5f5;
            border: none;
            width: 30px;
            height: 30px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            margin-left: 10px;
        }
        .edit-profile-btn i {
            color: #555;
        }
    </style>
</head>
<body>
    <h1 class="page-title">Perfil</h1>
    
    <div class="profile-card">
        <div class="profile-header">
            <div class="profile-pic">
                <img src="<%= user.photo %>" alt="<%= user.name %>">
            </div>
            <div class="profile-info">
                <div class="profile-name">
                    <%= user.name %> <span class="profile-id">#<%= String(user.id).substring(0, 4) %></span>
                </div>
                <div class="profile-details"><%= user.apelido || 'Sem apelido' %></div>
                <div class="profile-details"><%= user.email || 'Sem email' %></div>
                <div class="profile-details"><%= user.telefone || 'Sem telefone' %></div>
            </div>
        </div>
        
        <div class="user-groups">Meus Grupos:</div>
        <div class="empty-groups">Você não possui nenhum grupo</div>
    </div>
    
    <div class="history-card">
        <div class="history-title">Histórico de Reservas</div>
        <div class="empty-history">
            você não possui nenhum<br>
            histórico de reserva
        </div>
    </div>
    
    <%- include('partials/footer', {page: 'perfil'}) %>
</body>
</html> 